{
  "name": "spec-cli-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Spec CLI — 规范驱动开发命令行工具",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
